tables_schema = {
    "users":    """(
                    email       TEXT        PRIMARY KEY,
                    name        TEXT        NOT NULL,
                    age         INTEGER     NOT NULL,
                    password    TEXT        NOT NULL
                );"""

}
